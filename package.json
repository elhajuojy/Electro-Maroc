{
  "name": "tailwindcss",
  "version": "1.0.0",
  "description": "A utility-first CSS framework for rapid UI development.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tw:build": "npx tailwindcss -i ./public/assets/input.css -o ./public/dist/output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.2.4"
  }
}
